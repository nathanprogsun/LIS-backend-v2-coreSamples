// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"coresamples/ent/predicate"
	"coresamples/ent/userinvitationrecord"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserInvitationRecordQuery is the builder for querying UserInvitationRecord entities.
type UserInvitationRecordQuery struct {
	config
	ctx        *QueryContext
	order      []userinvitationrecord.OrderOption
	inters     []Interceptor
	predicates []predicate.UserInvitationRecord
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserInvitationRecordQuery builder.
func (uirq *UserInvitationRecordQuery) Where(ps ...predicate.UserInvitationRecord) *UserInvitationRecordQuery {
	uirq.predicates = append(uirq.predicates, ps...)
	return uirq
}

// Limit the number of records to be returned by this query.
func (uirq *UserInvitationRecordQuery) Limit(limit int) *UserInvitationRecordQuery {
	uirq.ctx.Limit = &limit
	return uirq
}

// Offset to start from.
func (uirq *UserInvitationRecordQuery) Offset(offset int) *UserInvitationRecordQuery {
	uirq.ctx.Offset = &offset
	return uirq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uirq *UserInvitationRecordQuery) Unique(unique bool) *UserInvitationRecordQuery {
	uirq.ctx.Unique = &unique
	return uirq
}

// Order specifies how the records should be ordered.
func (uirq *UserInvitationRecordQuery) Order(o ...userinvitationrecord.OrderOption) *UserInvitationRecordQuery {
	uirq.order = append(uirq.order, o...)
	return uirq
}

// First returns the first UserInvitationRecord entity from the query.
// Returns a *NotFoundError when no UserInvitationRecord was found.
func (uirq *UserInvitationRecordQuery) First(ctx context.Context) (*UserInvitationRecord, error) {
	nodes, err := uirq.Limit(1).All(setContextOp(ctx, uirq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userinvitationrecord.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uirq *UserInvitationRecordQuery) FirstX(ctx context.Context) *UserInvitationRecord {
	node, err := uirq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserInvitationRecord ID from the query.
// Returns a *NotFoundError when no UserInvitationRecord ID was found.
func (uirq *UserInvitationRecordQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uirq.Limit(1).IDs(setContextOp(ctx, uirq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userinvitationrecord.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uirq *UserInvitationRecordQuery) FirstIDX(ctx context.Context) int {
	id, err := uirq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserInvitationRecord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserInvitationRecord entity is found.
// Returns a *NotFoundError when no UserInvitationRecord entities are found.
func (uirq *UserInvitationRecordQuery) Only(ctx context.Context) (*UserInvitationRecord, error) {
	nodes, err := uirq.Limit(2).All(setContextOp(ctx, uirq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userinvitationrecord.Label}
	default:
		return nil, &NotSingularError{userinvitationrecord.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uirq *UserInvitationRecordQuery) OnlyX(ctx context.Context) *UserInvitationRecord {
	node, err := uirq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserInvitationRecord ID in the query.
// Returns a *NotSingularError when more than one UserInvitationRecord ID is found.
// Returns a *NotFoundError when no entities are found.
func (uirq *UserInvitationRecordQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uirq.Limit(2).IDs(setContextOp(ctx, uirq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userinvitationrecord.Label}
	default:
		err = &NotSingularError{userinvitationrecord.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uirq *UserInvitationRecordQuery) OnlyIDX(ctx context.Context) int {
	id, err := uirq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserInvitationRecords.
func (uirq *UserInvitationRecordQuery) All(ctx context.Context) ([]*UserInvitationRecord, error) {
	ctx = setContextOp(ctx, uirq.ctx, "All")
	if err := uirq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserInvitationRecord, *UserInvitationRecordQuery]()
	return withInterceptors[[]*UserInvitationRecord](ctx, uirq, qr, uirq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uirq *UserInvitationRecordQuery) AllX(ctx context.Context) []*UserInvitationRecord {
	nodes, err := uirq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserInvitationRecord IDs.
func (uirq *UserInvitationRecordQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uirq.ctx.Unique == nil && uirq.path != nil {
		uirq.Unique(true)
	}
	ctx = setContextOp(ctx, uirq.ctx, "IDs")
	if err = uirq.Select(userinvitationrecord.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uirq *UserInvitationRecordQuery) IDsX(ctx context.Context) []int {
	ids, err := uirq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uirq *UserInvitationRecordQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uirq.ctx, "Count")
	if err := uirq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uirq, querierCount[*UserInvitationRecordQuery](), uirq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uirq *UserInvitationRecordQuery) CountX(ctx context.Context) int {
	count, err := uirq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uirq *UserInvitationRecordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uirq.ctx, "Exist")
	switch _, err := uirq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uirq *UserInvitationRecordQuery) ExistX(ctx context.Context) bool {
	exist, err := uirq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserInvitationRecordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uirq *UserInvitationRecordQuery) Clone() *UserInvitationRecordQuery {
	if uirq == nil {
		return nil
	}
	return &UserInvitationRecordQuery{
		config:     uirq.config,
		ctx:        uirq.ctx.Clone(),
		order:      append([]userinvitationrecord.OrderOption{}, uirq.order...),
		inters:     append([]Interceptor{}, uirq.inters...),
		predicates: append([]predicate.UserInvitationRecord{}, uirq.predicates...),
		// clone intermediate query.
		sql:  uirq.sql.Clone(),
		path: uirq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CustomerID int `json:"customer_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserInvitationRecord.Query().
//		GroupBy(userinvitationrecord.FieldCustomerID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uirq *UserInvitationRecordQuery) GroupBy(field string, fields ...string) *UserInvitationRecordGroupBy {
	uirq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserInvitationRecordGroupBy{build: uirq}
	grbuild.flds = &uirq.ctx.Fields
	grbuild.label = userinvitationrecord.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CustomerID int `json:"customer_id,omitempty"`
//	}
//
//	client.UserInvitationRecord.Query().
//		Select(userinvitationrecord.FieldCustomerID).
//		Scan(ctx, &v)
func (uirq *UserInvitationRecordQuery) Select(fields ...string) *UserInvitationRecordSelect {
	uirq.ctx.Fields = append(uirq.ctx.Fields, fields...)
	sbuild := &UserInvitationRecordSelect{UserInvitationRecordQuery: uirq}
	sbuild.label = userinvitationrecord.Label
	sbuild.flds, sbuild.scan = &uirq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserInvitationRecordSelect configured with the given aggregations.
func (uirq *UserInvitationRecordQuery) Aggregate(fns ...AggregateFunc) *UserInvitationRecordSelect {
	return uirq.Select().Aggregate(fns...)
}

func (uirq *UserInvitationRecordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uirq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uirq); err != nil {
				return err
			}
		}
	}
	for _, f := range uirq.ctx.Fields {
		if !userinvitationrecord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uirq.path != nil {
		prev, err := uirq.path(ctx)
		if err != nil {
			return err
		}
		uirq.sql = prev
	}
	return nil
}

func (uirq *UserInvitationRecordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserInvitationRecord, error) {
	var (
		nodes = []*UserInvitationRecord{}
		_spec = uirq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserInvitationRecord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserInvitationRecord{config: uirq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uirq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uirq *UserInvitationRecordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uirq.querySpec()
	_spec.Node.Columns = uirq.ctx.Fields
	if len(uirq.ctx.Fields) > 0 {
		_spec.Unique = uirq.ctx.Unique != nil && *uirq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uirq.driver, _spec)
}

func (uirq *UserInvitationRecordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userinvitationrecord.Table, userinvitationrecord.Columns, sqlgraph.NewFieldSpec(userinvitationrecord.FieldID, field.TypeInt))
	_spec.From = uirq.sql
	if unique := uirq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uirq.path != nil {
		_spec.Unique = true
	}
	if fields := uirq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userinvitationrecord.FieldID)
		for i := range fields {
			if fields[i] != userinvitationrecord.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uirq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uirq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uirq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uirq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uirq *UserInvitationRecordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uirq.driver.Dialect())
	t1 := builder.Table(userinvitationrecord.Table)
	columns := uirq.ctx.Fields
	if len(columns) == 0 {
		columns = userinvitationrecord.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uirq.sql != nil {
		selector = uirq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uirq.ctx.Unique != nil && *uirq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uirq.predicates {
		p(selector)
	}
	for _, p := range uirq.order {
		p(selector)
	}
	if offset := uirq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uirq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserInvitationRecordGroupBy is the group-by builder for UserInvitationRecord entities.
type UserInvitationRecordGroupBy struct {
	selector
	build *UserInvitationRecordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uirgb *UserInvitationRecordGroupBy) Aggregate(fns ...AggregateFunc) *UserInvitationRecordGroupBy {
	uirgb.fns = append(uirgb.fns, fns...)
	return uirgb
}

// Scan applies the selector query and scans the result into the given value.
func (uirgb *UserInvitationRecordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uirgb.build.ctx, "GroupBy")
	if err := uirgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserInvitationRecordQuery, *UserInvitationRecordGroupBy](ctx, uirgb.build, uirgb, uirgb.build.inters, v)
}

func (uirgb *UserInvitationRecordGroupBy) sqlScan(ctx context.Context, root *UserInvitationRecordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uirgb.fns))
	for _, fn := range uirgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uirgb.flds)+len(uirgb.fns))
		for _, f := range *uirgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uirgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uirgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserInvitationRecordSelect is the builder for selecting fields of UserInvitationRecord entities.
type UserInvitationRecordSelect struct {
	*UserInvitationRecordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uirs *UserInvitationRecordSelect) Aggregate(fns ...AggregateFunc) *UserInvitationRecordSelect {
	uirs.fns = append(uirs.fns, fns...)
	return uirs
}

// Scan applies the selector query and scans the result into the given value.
func (uirs *UserInvitationRecordSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uirs.ctx, "Select")
	if err := uirs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserInvitationRecordQuery, *UserInvitationRecordSelect](ctx, uirs.UserInvitationRecordQuery, uirs, uirs.inters, v)
}

func (uirs *UserInvitationRecordSelect) sqlScan(ctx context.Context, root *UserInvitationRecordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uirs.fns))
	for _, fn := range uirs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uirs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uirs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
