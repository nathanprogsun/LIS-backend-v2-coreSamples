// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"coresamples/ent/predicate"
	"coresamples/ent/zipcode"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ZipcodeQuery is the builder for querying Zipcode entities.
type ZipcodeQuery struct {
	config
	ctx        *QueryContext
	order      []zipcode.OrderOption
	inters     []Interceptor
	predicates []predicate.Zipcode
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ZipcodeQuery builder.
func (zq *ZipcodeQuery) Where(ps ...predicate.Zipcode) *ZipcodeQuery {
	zq.predicates = append(zq.predicates, ps...)
	return zq
}

// Limit the number of records to be returned by this query.
func (zq *ZipcodeQuery) Limit(limit int) *ZipcodeQuery {
	zq.ctx.Limit = &limit
	return zq
}

// Offset to start from.
func (zq *ZipcodeQuery) Offset(offset int) *ZipcodeQuery {
	zq.ctx.Offset = &offset
	return zq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (zq *ZipcodeQuery) Unique(unique bool) *ZipcodeQuery {
	zq.ctx.Unique = &unique
	return zq
}

// Order specifies how the records should be ordered.
func (zq *ZipcodeQuery) Order(o ...zipcode.OrderOption) *ZipcodeQuery {
	zq.order = append(zq.order, o...)
	return zq
}

// First returns the first Zipcode entity from the query.
// Returns a *NotFoundError when no Zipcode was found.
func (zq *ZipcodeQuery) First(ctx context.Context) (*Zipcode, error) {
	nodes, err := zq.Limit(1).All(setContextOp(ctx, zq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{zipcode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (zq *ZipcodeQuery) FirstX(ctx context.Context) *Zipcode {
	node, err := zq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Zipcode ID from the query.
// Returns a *NotFoundError when no Zipcode ID was found.
func (zq *ZipcodeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = zq.Limit(1).IDs(setContextOp(ctx, zq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{zipcode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (zq *ZipcodeQuery) FirstIDX(ctx context.Context) int {
	id, err := zq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Zipcode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Zipcode entity is found.
// Returns a *NotFoundError when no Zipcode entities are found.
func (zq *ZipcodeQuery) Only(ctx context.Context) (*Zipcode, error) {
	nodes, err := zq.Limit(2).All(setContextOp(ctx, zq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{zipcode.Label}
	default:
		return nil, &NotSingularError{zipcode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (zq *ZipcodeQuery) OnlyX(ctx context.Context) *Zipcode {
	node, err := zq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Zipcode ID in the query.
// Returns a *NotSingularError when more than one Zipcode ID is found.
// Returns a *NotFoundError when no entities are found.
func (zq *ZipcodeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = zq.Limit(2).IDs(setContextOp(ctx, zq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{zipcode.Label}
	default:
		err = &NotSingularError{zipcode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (zq *ZipcodeQuery) OnlyIDX(ctx context.Context) int {
	id, err := zq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Zipcodes.
func (zq *ZipcodeQuery) All(ctx context.Context) ([]*Zipcode, error) {
	ctx = setContextOp(ctx, zq.ctx, "All")
	if err := zq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Zipcode, *ZipcodeQuery]()
	return withInterceptors[[]*Zipcode](ctx, zq, qr, zq.inters)
}

// AllX is like All, but panics if an error occurs.
func (zq *ZipcodeQuery) AllX(ctx context.Context) []*Zipcode {
	nodes, err := zq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Zipcode IDs.
func (zq *ZipcodeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if zq.ctx.Unique == nil && zq.path != nil {
		zq.Unique(true)
	}
	ctx = setContextOp(ctx, zq.ctx, "IDs")
	if err = zq.Select(zipcode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (zq *ZipcodeQuery) IDsX(ctx context.Context) []int {
	ids, err := zq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (zq *ZipcodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, zq.ctx, "Count")
	if err := zq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, zq, querierCount[*ZipcodeQuery](), zq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (zq *ZipcodeQuery) CountX(ctx context.Context) int {
	count, err := zq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (zq *ZipcodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, zq.ctx, "Exist")
	switch _, err := zq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (zq *ZipcodeQuery) ExistX(ctx context.Context) bool {
	exist, err := zq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ZipcodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (zq *ZipcodeQuery) Clone() *ZipcodeQuery {
	if zq == nil {
		return nil
	}
	return &ZipcodeQuery{
		config:     zq.config,
		ctx:        zq.ctx.Clone(),
		order:      append([]zipcode.OrderOption{}, zq.order...),
		inters:     append([]Interceptor{}, zq.inters...),
		predicates: append([]predicate.Zipcode{}, zq.predicates...),
		// clone intermediate query.
		sql:  zq.sql.Clone(),
		path: zq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ZipCodeType string `json:"ZipCodeType,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Zipcode.Query().
//		GroupBy(zipcode.FieldZipCodeType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (zq *ZipcodeQuery) GroupBy(field string, fields ...string) *ZipcodeGroupBy {
	zq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ZipcodeGroupBy{build: zq}
	grbuild.flds = &zq.ctx.Fields
	grbuild.label = zipcode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ZipCodeType string `json:"ZipCodeType,omitempty"`
//	}
//
//	client.Zipcode.Query().
//		Select(zipcode.FieldZipCodeType).
//		Scan(ctx, &v)
func (zq *ZipcodeQuery) Select(fields ...string) *ZipcodeSelect {
	zq.ctx.Fields = append(zq.ctx.Fields, fields...)
	sbuild := &ZipcodeSelect{ZipcodeQuery: zq}
	sbuild.label = zipcode.Label
	sbuild.flds, sbuild.scan = &zq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ZipcodeSelect configured with the given aggregations.
func (zq *ZipcodeQuery) Aggregate(fns ...AggregateFunc) *ZipcodeSelect {
	return zq.Select().Aggregate(fns...)
}

func (zq *ZipcodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range zq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, zq); err != nil {
				return err
			}
		}
	}
	for _, f := range zq.ctx.Fields {
		if !zipcode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if zq.path != nil {
		prev, err := zq.path(ctx)
		if err != nil {
			return err
		}
		zq.sql = prev
	}
	return nil
}

func (zq *ZipcodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Zipcode, error) {
	var (
		nodes = []*Zipcode{}
		_spec = zq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Zipcode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Zipcode{config: zq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, zq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (zq *ZipcodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := zq.querySpec()
	_spec.Node.Columns = zq.ctx.Fields
	if len(zq.ctx.Fields) > 0 {
		_spec.Unique = zq.ctx.Unique != nil && *zq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, zq.driver, _spec)
}

func (zq *ZipcodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(zipcode.Table, zipcode.Columns, sqlgraph.NewFieldSpec(zipcode.FieldID, field.TypeInt))
	_spec.From = zq.sql
	if unique := zq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if zq.path != nil {
		_spec.Unique = true
	}
	if fields := zq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, zipcode.FieldID)
		for i := range fields {
			if fields[i] != zipcode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := zq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := zq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := zq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := zq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (zq *ZipcodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(zq.driver.Dialect())
	t1 := builder.Table(zipcode.Table)
	columns := zq.ctx.Fields
	if len(columns) == 0 {
		columns = zipcode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if zq.sql != nil {
		selector = zq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if zq.ctx.Unique != nil && *zq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range zq.predicates {
		p(selector)
	}
	for _, p := range zq.order {
		p(selector)
	}
	if offset := zq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := zq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ZipcodeGroupBy is the group-by builder for Zipcode entities.
type ZipcodeGroupBy struct {
	selector
	build *ZipcodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (zgb *ZipcodeGroupBy) Aggregate(fns ...AggregateFunc) *ZipcodeGroupBy {
	zgb.fns = append(zgb.fns, fns...)
	return zgb
}

// Scan applies the selector query and scans the result into the given value.
func (zgb *ZipcodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, zgb.build.ctx, "GroupBy")
	if err := zgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ZipcodeQuery, *ZipcodeGroupBy](ctx, zgb.build, zgb, zgb.build.inters, v)
}

func (zgb *ZipcodeGroupBy) sqlScan(ctx context.Context, root *ZipcodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(zgb.fns))
	for _, fn := range zgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*zgb.flds)+len(zgb.fns))
		for _, f := range *zgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*zgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := zgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ZipcodeSelect is the builder for selecting fields of Zipcode entities.
type ZipcodeSelect struct {
	*ZipcodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (zs *ZipcodeSelect) Aggregate(fns ...AggregateFunc) *ZipcodeSelect {
	zs.fns = append(zs.fns, fns...)
	return zs
}

// Scan applies the selector query and scans the result into the given value.
func (zs *ZipcodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, zs.ctx, "Select")
	if err := zs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ZipcodeQuery, *ZipcodeSelect](ctx, zs.ZipcodeQuery, zs, zs.inters, v)
}

func (zs *ZipcodeSelect) sqlScan(ctx context.Context, root *ZipcodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(zs.fns))
	for _, fn := range zs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*zs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := zs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
