// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"coresamples/ent/patientflag"
	"coresamples/ent/predicate"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PatientFlagQuery is the builder for querying PatientFlag entities.
type PatientFlagQuery struct {
	config
	ctx        *QueryContext
	order      []patientflag.OrderOption
	inters     []Interceptor
	predicates []predicate.PatientFlag
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PatientFlagQuery builder.
func (pfq *PatientFlagQuery) Where(ps ...predicate.PatientFlag) *PatientFlagQuery {
	pfq.predicates = append(pfq.predicates, ps...)
	return pfq
}

// Limit the number of records to be returned by this query.
func (pfq *PatientFlagQuery) Limit(limit int) *PatientFlagQuery {
	pfq.ctx.Limit = &limit
	return pfq
}

// Offset to start from.
func (pfq *PatientFlagQuery) Offset(offset int) *PatientFlagQuery {
	pfq.ctx.Offset = &offset
	return pfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pfq *PatientFlagQuery) Unique(unique bool) *PatientFlagQuery {
	pfq.ctx.Unique = &unique
	return pfq
}

// Order specifies how the records should be ordered.
func (pfq *PatientFlagQuery) Order(o ...patientflag.OrderOption) *PatientFlagQuery {
	pfq.order = append(pfq.order, o...)
	return pfq
}

// First returns the first PatientFlag entity from the query.
// Returns a *NotFoundError when no PatientFlag was found.
func (pfq *PatientFlagQuery) First(ctx context.Context) (*PatientFlag, error) {
	nodes, err := pfq.Limit(1).All(setContextOp(ctx, pfq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{patientflag.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pfq *PatientFlagQuery) FirstX(ctx context.Context) *PatientFlag {
	node, err := pfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PatientFlag ID from the query.
// Returns a *NotFoundError when no PatientFlag ID was found.
func (pfq *PatientFlagQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pfq.Limit(1).IDs(setContextOp(ctx, pfq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{patientflag.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pfq *PatientFlagQuery) FirstIDX(ctx context.Context) int {
	id, err := pfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PatientFlag entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PatientFlag entity is found.
// Returns a *NotFoundError when no PatientFlag entities are found.
func (pfq *PatientFlagQuery) Only(ctx context.Context) (*PatientFlag, error) {
	nodes, err := pfq.Limit(2).All(setContextOp(ctx, pfq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{patientflag.Label}
	default:
		return nil, &NotSingularError{patientflag.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pfq *PatientFlagQuery) OnlyX(ctx context.Context) *PatientFlag {
	node, err := pfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PatientFlag ID in the query.
// Returns a *NotSingularError when more than one PatientFlag ID is found.
// Returns a *NotFoundError when no entities are found.
func (pfq *PatientFlagQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pfq.Limit(2).IDs(setContextOp(ctx, pfq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{patientflag.Label}
	default:
		err = &NotSingularError{patientflag.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pfq *PatientFlagQuery) OnlyIDX(ctx context.Context) int {
	id, err := pfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PatientFlags.
func (pfq *PatientFlagQuery) All(ctx context.Context) ([]*PatientFlag, error) {
	ctx = setContextOp(ctx, pfq.ctx, "All")
	if err := pfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PatientFlag, *PatientFlagQuery]()
	return withInterceptors[[]*PatientFlag](ctx, pfq, qr, pfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pfq *PatientFlagQuery) AllX(ctx context.Context) []*PatientFlag {
	nodes, err := pfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PatientFlag IDs.
func (pfq *PatientFlagQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pfq.ctx.Unique == nil && pfq.path != nil {
		pfq.Unique(true)
	}
	ctx = setContextOp(ctx, pfq.ctx, "IDs")
	if err = pfq.Select(patientflag.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pfq *PatientFlagQuery) IDsX(ctx context.Context) []int {
	ids, err := pfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pfq *PatientFlagQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pfq.ctx, "Count")
	if err := pfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pfq, querierCount[*PatientFlagQuery](), pfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pfq *PatientFlagQuery) CountX(ctx context.Context) int {
	count, err := pfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pfq *PatientFlagQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pfq.ctx, "Exist")
	switch _, err := pfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pfq *PatientFlagQuery) ExistX(ctx context.Context) bool {
	exist, err := pfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PatientFlagQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pfq *PatientFlagQuery) Clone() *PatientFlagQuery {
	if pfq == nil {
		return nil
	}
	return &PatientFlagQuery{
		config:     pfq.config,
		ctx:        pfq.ctx.Clone(),
		order:      append([]patientflag.OrderOption{}, pfq.order...),
		inters:     append([]Interceptor{}, pfq.inters...),
		predicates: append([]predicate.PatientFlag{}, pfq.predicates...),
		// clone intermediate query.
		sql:  pfq.sql.Clone(),
		path: pfq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PatientFlagName string `json:"patient_flag_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PatientFlag.Query().
//		GroupBy(patientflag.FieldPatientFlagName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pfq *PatientFlagQuery) GroupBy(field string, fields ...string) *PatientFlagGroupBy {
	pfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PatientFlagGroupBy{build: pfq}
	grbuild.flds = &pfq.ctx.Fields
	grbuild.label = patientflag.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PatientFlagName string `json:"patient_flag_name,omitempty"`
//	}
//
//	client.PatientFlag.Query().
//		Select(patientflag.FieldPatientFlagName).
//		Scan(ctx, &v)
func (pfq *PatientFlagQuery) Select(fields ...string) *PatientFlagSelect {
	pfq.ctx.Fields = append(pfq.ctx.Fields, fields...)
	sbuild := &PatientFlagSelect{PatientFlagQuery: pfq}
	sbuild.label = patientflag.Label
	sbuild.flds, sbuild.scan = &pfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PatientFlagSelect configured with the given aggregations.
func (pfq *PatientFlagQuery) Aggregate(fns ...AggregateFunc) *PatientFlagSelect {
	return pfq.Select().Aggregate(fns...)
}

func (pfq *PatientFlagQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pfq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pfq); err != nil {
				return err
			}
		}
	}
	for _, f := range pfq.ctx.Fields {
		if !patientflag.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pfq.path != nil {
		prev, err := pfq.path(ctx)
		if err != nil {
			return err
		}
		pfq.sql = prev
	}
	return nil
}

func (pfq *PatientFlagQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PatientFlag, error) {
	var (
		nodes = []*PatientFlag{}
		_spec = pfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PatientFlag).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PatientFlag{config: pfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pfq *PatientFlagQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pfq.querySpec()
	_spec.Node.Columns = pfq.ctx.Fields
	if len(pfq.ctx.Fields) > 0 {
		_spec.Unique = pfq.ctx.Unique != nil && *pfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pfq.driver, _spec)
}

func (pfq *PatientFlagQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(patientflag.Table, patientflag.Columns, sqlgraph.NewFieldSpec(patientflag.FieldID, field.TypeInt))
	_spec.From = pfq.sql
	if unique := pfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pfq.path != nil {
		_spec.Unique = true
	}
	if fields := pfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, patientflag.FieldID)
		for i := range fields {
			if fields[i] != patientflag.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pfq *PatientFlagQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pfq.driver.Dialect())
	t1 := builder.Table(patientflag.Table)
	columns := pfq.ctx.Fields
	if len(columns) == 0 {
		columns = patientflag.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pfq.sql != nil {
		selector = pfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pfq.ctx.Unique != nil && *pfq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pfq.predicates {
		p(selector)
	}
	for _, p := range pfq.order {
		p(selector)
	}
	if offset := pfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PatientFlagGroupBy is the group-by builder for PatientFlag entities.
type PatientFlagGroupBy struct {
	selector
	build *PatientFlagQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pfgb *PatientFlagGroupBy) Aggregate(fns ...AggregateFunc) *PatientFlagGroupBy {
	pfgb.fns = append(pfgb.fns, fns...)
	return pfgb
}

// Scan applies the selector query and scans the result into the given value.
func (pfgb *PatientFlagGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pfgb.build.ctx, "GroupBy")
	if err := pfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PatientFlagQuery, *PatientFlagGroupBy](ctx, pfgb.build, pfgb, pfgb.build.inters, v)
}

func (pfgb *PatientFlagGroupBy) sqlScan(ctx context.Context, root *PatientFlagQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pfgb.fns))
	for _, fn := range pfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pfgb.flds)+len(pfgb.fns))
		for _, f := range *pfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PatientFlagSelect is the builder for selecting fields of PatientFlag entities.
type PatientFlagSelect struct {
	*PatientFlagQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pfs *PatientFlagSelect) Aggregate(fns ...AggregateFunc) *PatientFlagSelect {
	pfs.fns = append(pfs.fns, fns...)
	return pfs
}

// Scan applies the selector query and scans the result into the given value.
func (pfs *PatientFlagSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pfs.ctx, "Select")
	if err := pfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PatientFlagQuery, *PatientFlagSelect](ctx, pfs.PatientFlagQuery, pfs, pfs.inters, v)
}

func (pfs *PatientFlagSelect) sqlScan(ctx context.Context, root *PatientFlagQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pfs.fns))
	for _, fn := range pfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
