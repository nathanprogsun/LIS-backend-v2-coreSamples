// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"coresamples/ent/predicate"
	"coresamples/ent/salesteam"
	"coresamples/ent/salestitle"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SalesTitleQuery is the builder for querying SalesTitle entities.
type SalesTitleQuery struct {
	config
	ctx        *QueryContext
	order      []salestitle.OrderOption
	inters     []Interceptor
	predicates []predicate.SalesTitle
	withSales  *SalesTeamQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SalesTitleQuery builder.
func (stq *SalesTitleQuery) Where(ps ...predicate.SalesTitle) *SalesTitleQuery {
	stq.predicates = append(stq.predicates, ps...)
	return stq
}

// Limit the number of records to be returned by this query.
func (stq *SalesTitleQuery) Limit(limit int) *SalesTitleQuery {
	stq.ctx.Limit = &limit
	return stq
}

// Offset to start from.
func (stq *SalesTitleQuery) Offset(offset int) *SalesTitleQuery {
	stq.ctx.Offset = &offset
	return stq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (stq *SalesTitleQuery) Unique(unique bool) *SalesTitleQuery {
	stq.ctx.Unique = &unique
	return stq
}

// Order specifies how the records should be ordered.
func (stq *SalesTitleQuery) Order(o ...salestitle.OrderOption) *SalesTitleQuery {
	stq.order = append(stq.order, o...)
	return stq
}

// QuerySales chains the current query on the "sales" edge.
func (stq *SalesTitleQuery) QuerySales() *SalesTeamQuery {
	query := (&SalesTeamClient{config: stq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := stq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := stq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(salestitle.Table, salestitle.FieldID, selector),
			sqlgraph.To(salesteam.Table, salesteam.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, salestitle.SalesTable, salestitle.SalesColumn),
		)
		fromU = sqlgraph.SetNeighbors(stq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SalesTitle entity from the query.
// Returns a *NotFoundError when no SalesTitle was found.
func (stq *SalesTitleQuery) First(ctx context.Context) (*SalesTitle, error) {
	nodes, err := stq.Limit(1).All(setContextOp(ctx, stq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{salestitle.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (stq *SalesTitleQuery) FirstX(ctx context.Context) *SalesTitle {
	node, err := stq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SalesTitle ID from the query.
// Returns a *NotFoundError when no SalesTitle ID was found.
func (stq *SalesTitleQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = stq.Limit(1).IDs(setContextOp(ctx, stq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{salestitle.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (stq *SalesTitleQuery) FirstIDX(ctx context.Context) int {
	id, err := stq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SalesTitle entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SalesTitle entity is found.
// Returns a *NotFoundError when no SalesTitle entities are found.
func (stq *SalesTitleQuery) Only(ctx context.Context) (*SalesTitle, error) {
	nodes, err := stq.Limit(2).All(setContextOp(ctx, stq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{salestitle.Label}
	default:
		return nil, &NotSingularError{salestitle.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (stq *SalesTitleQuery) OnlyX(ctx context.Context) *SalesTitle {
	node, err := stq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SalesTitle ID in the query.
// Returns a *NotSingularError when more than one SalesTitle ID is found.
// Returns a *NotFoundError when no entities are found.
func (stq *SalesTitleQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = stq.Limit(2).IDs(setContextOp(ctx, stq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{salestitle.Label}
	default:
		err = &NotSingularError{salestitle.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (stq *SalesTitleQuery) OnlyIDX(ctx context.Context) int {
	id, err := stq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SalesTitles.
func (stq *SalesTitleQuery) All(ctx context.Context) ([]*SalesTitle, error) {
	ctx = setContextOp(ctx, stq.ctx, "All")
	if err := stq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SalesTitle, *SalesTitleQuery]()
	return withInterceptors[[]*SalesTitle](ctx, stq, qr, stq.inters)
}

// AllX is like All, but panics if an error occurs.
func (stq *SalesTitleQuery) AllX(ctx context.Context) []*SalesTitle {
	nodes, err := stq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SalesTitle IDs.
func (stq *SalesTitleQuery) IDs(ctx context.Context) (ids []int, err error) {
	if stq.ctx.Unique == nil && stq.path != nil {
		stq.Unique(true)
	}
	ctx = setContextOp(ctx, stq.ctx, "IDs")
	if err = stq.Select(salestitle.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (stq *SalesTitleQuery) IDsX(ctx context.Context) []int {
	ids, err := stq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (stq *SalesTitleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, stq.ctx, "Count")
	if err := stq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, stq, querierCount[*SalesTitleQuery](), stq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (stq *SalesTitleQuery) CountX(ctx context.Context) int {
	count, err := stq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (stq *SalesTitleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, stq.ctx, "Exist")
	switch _, err := stq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (stq *SalesTitleQuery) ExistX(ctx context.Context) bool {
	exist, err := stq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SalesTitleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (stq *SalesTitleQuery) Clone() *SalesTitleQuery {
	if stq == nil {
		return nil
	}
	return &SalesTitleQuery{
		config:     stq.config,
		ctx:        stq.ctx.Clone(),
		order:      append([]salestitle.OrderOption{}, stq.order...),
		inters:     append([]Interceptor{}, stq.inters...),
		predicates: append([]predicate.SalesTitle{}, stq.predicates...),
		withSales:  stq.withSales.Clone(),
		// clone intermediate query.
		sql:  stq.sql.Clone(),
		path: stq.path,
	}
}

// WithSales tells the query-builder to eager-load the nodes that are connected to
// the "sales" edge. The optional arguments are used to configure the query builder of the edge.
func (stq *SalesTitleQuery) WithSales(opts ...func(*SalesTeamQuery)) *SalesTitleQuery {
	query := (&SalesTeamClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	stq.withSales = query
	return stq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TitleName string `json:"title_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SalesTitle.Query().
//		GroupBy(salestitle.FieldTitleName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (stq *SalesTitleQuery) GroupBy(field string, fields ...string) *SalesTitleGroupBy {
	stq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SalesTitleGroupBy{build: stq}
	grbuild.flds = &stq.ctx.Fields
	grbuild.label = salestitle.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TitleName string `json:"title_name,omitempty"`
//	}
//
//	client.SalesTitle.Query().
//		Select(salestitle.FieldTitleName).
//		Scan(ctx, &v)
func (stq *SalesTitleQuery) Select(fields ...string) *SalesTitleSelect {
	stq.ctx.Fields = append(stq.ctx.Fields, fields...)
	sbuild := &SalesTitleSelect{SalesTitleQuery: stq}
	sbuild.label = salestitle.Label
	sbuild.flds, sbuild.scan = &stq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SalesTitleSelect configured with the given aggregations.
func (stq *SalesTitleQuery) Aggregate(fns ...AggregateFunc) *SalesTitleSelect {
	return stq.Select().Aggregate(fns...)
}

func (stq *SalesTitleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range stq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, stq); err != nil {
				return err
			}
		}
	}
	for _, f := range stq.ctx.Fields {
		if !salestitle.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if stq.path != nil {
		prev, err := stq.path(ctx)
		if err != nil {
			return err
		}
		stq.sql = prev
	}
	return nil
}

func (stq *SalesTitleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SalesTitle, error) {
	var (
		nodes       = []*SalesTitle{}
		_spec       = stq.querySpec()
		loadedTypes = [1]bool{
			stq.withSales != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SalesTitle).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SalesTitle{config: stq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, stq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := stq.withSales; query != nil {
		if err := stq.loadSales(ctx, query, nodes,
			func(n *SalesTitle) { n.Edges.Sales = []*SalesTeam{} },
			func(n *SalesTitle, e *SalesTeam) { n.Edges.Sales = append(n.Edges.Sales, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (stq *SalesTitleQuery) loadSales(ctx context.Context, query *SalesTeamQuery, nodes []*SalesTitle, init func(*SalesTitle), assign func(*SalesTitle, *SalesTeam)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*SalesTitle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(salesteam.FieldTitleID)
	}
	query.Where(predicate.SalesTeam(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(salestitle.SalesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.TitleID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "title_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (stq *SalesTitleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := stq.querySpec()
	_spec.Node.Columns = stq.ctx.Fields
	if len(stq.ctx.Fields) > 0 {
		_spec.Unique = stq.ctx.Unique != nil && *stq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, stq.driver, _spec)
}

func (stq *SalesTitleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(salestitle.Table, salestitle.Columns, sqlgraph.NewFieldSpec(salestitle.FieldID, field.TypeInt))
	_spec.From = stq.sql
	if unique := stq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if stq.path != nil {
		_spec.Unique = true
	}
	if fields := stq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, salestitle.FieldID)
		for i := range fields {
			if fields[i] != salestitle.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := stq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := stq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := stq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := stq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (stq *SalesTitleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(stq.driver.Dialect())
	t1 := builder.Table(salestitle.Table)
	columns := stq.ctx.Fields
	if len(columns) == 0 {
		columns = salestitle.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if stq.sql != nil {
		selector = stq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if stq.ctx.Unique != nil && *stq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range stq.predicates {
		p(selector)
	}
	for _, p := range stq.order {
		p(selector)
	}
	if offset := stq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := stq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SalesTitleGroupBy is the group-by builder for SalesTitle entities.
type SalesTitleGroupBy struct {
	selector
	build *SalesTitleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (stgb *SalesTitleGroupBy) Aggregate(fns ...AggregateFunc) *SalesTitleGroupBy {
	stgb.fns = append(stgb.fns, fns...)
	return stgb
}

// Scan applies the selector query and scans the result into the given value.
func (stgb *SalesTitleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, stgb.build.ctx, "GroupBy")
	if err := stgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SalesTitleQuery, *SalesTitleGroupBy](ctx, stgb.build, stgb, stgb.build.inters, v)
}

func (stgb *SalesTitleGroupBy) sqlScan(ctx context.Context, root *SalesTitleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(stgb.fns))
	for _, fn := range stgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*stgb.flds)+len(stgb.fns))
		for _, f := range *stgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*stgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := stgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SalesTitleSelect is the builder for selecting fields of SalesTitle entities.
type SalesTitleSelect struct {
	*SalesTitleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sts *SalesTitleSelect) Aggregate(fns ...AggregateFunc) *SalesTitleSelect {
	sts.fns = append(sts.fns, fns...)
	return sts
}

// Scan applies the selector query and scans the result into the given value.
func (sts *SalesTitleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sts.ctx, "Select")
	if err := sts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SalesTitleQuery, *SalesTitleSelect](ctx, sts.SalesTitleQuery, sts, sts.inters, v)
}

func (sts *SalesTitleSelect) sqlScan(ctx context.Context, root *SalesTitleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sts.fns))
	for _, fn := range sts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
