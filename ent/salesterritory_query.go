// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"coresamples/ent/predicate"
	"coresamples/ent/salesterritory"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SalesTerritoryQuery is the builder for querying SalesTerritory entities.
type SalesTerritoryQuery struct {
	config
	ctx        *QueryContext
	order      []salesterritory.OrderOption
	inters     []Interceptor
	predicates []predicate.SalesTerritory
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SalesTerritoryQuery builder.
func (stq *SalesTerritoryQuery) Where(ps ...predicate.SalesTerritory) *SalesTerritoryQuery {
	stq.predicates = append(stq.predicates, ps...)
	return stq
}

// Limit the number of records to be returned by this query.
func (stq *SalesTerritoryQuery) Limit(limit int) *SalesTerritoryQuery {
	stq.ctx.Limit = &limit
	return stq
}

// Offset to start from.
func (stq *SalesTerritoryQuery) Offset(offset int) *SalesTerritoryQuery {
	stq.ctx.Offset = &offset
	return stq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (stq *SalesTerritoryQuery) Unique(unique bool) *SalesTerritoryQuery {
	stq.ctx.Unique = &unique
	return stq
}

// Order specifies how the records should be ordered.
func (stq *SalesTerritoryQuery) Order(o ...salesterritory.OrderOption) *SalesTerritoryQuery {
	stq.order = append(stq.order, o...)
	return stq
}

// First returns the first SalesTerritory entity from the query.
// Returns a *NotFoundError when no SalesTerritory was found.
func (stq *SalesTerritoryQuery) First(ctx context.Context) (*SalesTerritory, error) {
	nodes, err := stq.Limit(1).All(setContextOp(ctx, stq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{salesterritory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (stq *SalesTerritoryQuery) FirstX(ctx context.Context) *SalesTerritory {
	node, err := stq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SalesTerritory ID from the query.
// Returns a *NotFoundError when no SalesTerritory ID was found.
func (stq *SalesTerritoryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = stq.Limit(1).IDs(setContextOp(ctx, stq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{salesterritory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (stq *SalesTerritoryQuery) FirstIDX(ctx context.Context) int {
	id, err := stq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SalesTerritory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SalesTerritory entity is found.
// Returns a *NotFoundError when no SalesTerritory entities are found.
func (stq *SalesTerritoryQuery) Only(ctx context.Context) (*SalesTerritory, error) {
	nodes, err := stq.Limit(2).All(setContextOp(ctx, stq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{salesterritory.Label}
	default:
		return nil, &NotSingularError{salesterritory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (stq *SalesTerritoryQuery) OnlyX(ctx context.Context) *SalesTerritory {
	node, err := stq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SalesTerritory ID in the query.
// Returns a *NotSingularError when more than one SalesTerritory ID is found.
// Returns a *NotFoundError when no entities are found.
func (stq *SalesTerritoryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = stq.Limit(2).IDs(setContextOp(ctx, stq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{salesterritory.Label}
	default:
		err = &NotSingularError{salesterritory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (stq *SalesTerritoryQuery) OnlyIDX(ctx context.Context) int {
	id, err := stq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SalesTerritories.
func (stq *SalesTerritoryQuery) All(ctx context.Context) ([]*SalesTerritory, error) {
	ctx = setContextOp(ctx, stq.ctx, "All")
	if err := stq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SalesTerritory, *SalesTerritoryQuery]()
	return withInterceptors[[]*SalesTerritory](ctx, stq, qr, stq.inters)
}

// AllX is like All, but panics if an error occurs.
func (stq *SalesTerritoryQuery) AllX(ctx context.Context) []*SalesTerritory {
	nodes, err := stq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SalesTerritory IDs.
func (stq *SalesTerritoryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if stq.ctx.Unique == nil && stq.path != nil {
		stq.Unique(true)
	}
	ctx = setContextOp(ctx, stq.ctx, "IDs")
	if err = stq.Select(salesterritory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (stq *SalesTerritoryQuery) IDsX(ctx context.Context) []int {
	ids, err := stq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (stq *SalesTerritoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, stq.ctx, "Count")
	if err := stq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, stq, querierCount[*SalesTerritoryQuery](), stq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (stq *SalesTerritoryQuery) CountX(ctx context.Context) int {
	count, err := stq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (stq *SalesTerritoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, stq.ctx, "Exist")
	switch _, err := stq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (stq *SalesTerritoryQuery) ExistX(ctx context.Context) bool {
	exist, err := stq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SalesTerritoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (stq *SalesTerritoryQuery) Clone() *SalesTerritoryQuery {
	if stq == nil {
		return nil
	}
	return &SalesTerritoryQuery{
		config:     stq.config,
		ctx:        stq.ctx.Clone(),
		order:      append([]salesterritory.OrderOption{}, stq.order...),
		inters:     append([]Interceptor{}, stq.inters...),
		predicates: append([]predicate.SalesTerritory{}, stq.predicates...),
		// clone intermediate query.
		sql:  stq.sql.Clone(),
		path: stq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Sales string `json:"sales,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SalesTerritory.Query().
//		GroupBy(salesterritory.FieldSales).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (stq *SalesTerritoryQuery) GroupBy(field string, fields ...string) *SalesTerritoryGroupBy {
	stq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SalesTerritoryGroupBy{build: stq}
	grbuild.flds = &stq.ctx.Fields
	grbuild.label = salesterritory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Sales string `json:"sales,omitempty"`
//	}
//
//	client.SalesTerritory.Query().
//		Select(salesterritory.FieldSales).
//		Scan(ctx, &v)
func (stq *SalesTerritoryQuery) Select(fields ...string) *SalesTerritorySelect {
	stq.ctx.Fields = append(stq.ctx.Fields, fields...)
	sbuild := &SalesTerritorySelect{SalesTerritoryQuery: stq}
	sbuild.label = salesterritory.Label
	sbuild.flds, sbuild.scan = &stq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SalesTerritorySelect configured with the given aggregations.
func (stq *SalesTerritoryQuery) Aggregate(fns ...AggregateFunc) *SalesTerritorySelect {
	return stq.Select().Aggregate(fns...)
}

func (stq *SalesTerritoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range stq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, stq); err != nil {
				return err
			}
		}
	}
	for _, f := range stq.ctx.Fields {
		if !salesterritory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if stq.path != nil {
		prev, err := stq.path(ctx)
		if err != nil {
			return err
		}
		stq.sql = prev
	}
	return nil
}

func (stq *SalesTerritoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SalesTerritory, error) {
	var (
		nodes = []*SalesTerritory{}
		_spec = stq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SalesTerritory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SalesTerritory{config: stq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, stq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (stq *SalesTerritoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := stq.querySpec()
	_spec.Node.Columns = stq.ctx.Fields
	if len(stq.ctx.Fields) > 0 {
		_spec.Unique = stq.ctx.Unique != nil && *stq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, stq.driver, _spec)
}

func (stq *SalesTerritoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(salesterritory.Table, salesterritory.Columns, sqlgraph.NewFieldSpec(salesterritory.FieldID, field.TypeInt))
	_spec.From = stq.sql
	if unique := stq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if stq.path != nil {
		_spec.Unique = true
	}
	if fields := stq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, salesterritory.FieldID)
		for i := range fields {
			if fields[i] != salesterritory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := stq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := stq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := stq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := stq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (stq *SalesTerritoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(stq.driver.Dialect())
	t1 := builder.Table(salesterritory.Table)
	columns := stq.ctx.Fields
	if len(columns) == 0 {
		columns = salesterritory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if stq.sql != nil {
		selector = stq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if stq.ctx.Unique != nil && *stq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range stq.predicates {
		p(selector)
	}
	for _, p := range stq.order {
		p(selector)
	}
	if offset := stq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := stq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SalesTerritoryGroupBy is the group-by builder for SalesTerritory entities.
type SalesTerritoryGroupBy struct {
	selector
	build *SalesTerritoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (stgb *SalesTerritoryGroupBy) Aggregate(fns ...AggregateFunc) *SalesTerritoryGroupBy {
	stgb.fns = append(stgb.fns, fns...)
	return stgb
}

// Scan applies the selector query and scans the result into the given value.
func (stgb *SalesTerritoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, stgb.build.ctx, "GroupBy")
	if err := stgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SalesTerritoryQuery, *SalesTerritoryGroupBy](ctx, stgb.build, stgb, stgb.build.inters, v)
}

func (stgb *SalesTerritoryGroupBy) sqlScan(ctx context.Context, root *SalesTerritoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(stgb.fns))
	for _, fn := range stgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*stgb.flds)+len(stgb.fns))
		for _, f := range *stgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*stgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := stgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SalesTerritorySelect is the builder for selecting fields of SalesTerritory entities.
type SalesTerritorySelect struct {
	*SalesTerritoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sts *SalesTerritorySelect) Aggregate(fns ...AggregateFunc) *SalesTerritorySelect {
	sts.fns = append(sts.fns, fns...)
	return sts
}

// Scan applies the selector query and scans the result into the given value.
func (sts *SalesTerritorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sts.ctx, "Select")
	if err := sts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SalesTerritoryQuery, *SalesTerritorySelect](ctx, sts.SalesTerritoryQuery, sts, sts.inters, v)
}

func (sts *SalesTerritorySelect) sqlScan(ctx context.Context, root *SalesTerritoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sts.fns))
	for _, fn := range sts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
