// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"coresamples/ent/address"
	"coresamples/ent/clinic"
	"coresamples/ent/contact"
	"coresamples/ent/customer"
	"coresamples/ent/patient"
	"coresamples/ent/patientweightheight"
	"coresamples/ent/predicate"
	"coresamples/ent/sample"
	"coresamples/ent/setting"
	"coresamples/ent/user"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PatientQuery is the builder for querying Patient entities.
type PatientQuery struct {
	config
	ctx                            *QueryContext
	order                          []patient.OrderOption
	inters                         []Interceptor
	predicates                     []predicate.Patient
	withCurrentCustomer            *CustomerQuery
	withPatientCustomers           *CustomerQuery
	withSamples                    *SampleQuery
	withPatientContacts            *ContactQuery
	withPatientAddresses           *AddressQuery
	withPatientClinics             *ClinicQuery
	withUser                       *UserQuery
	withPatientWeightHeightHistory *PatientWeightHeightQuery
	withPatientSettings            *SettingQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PatientQuery builder.
func (pq *PatientQuery) Where(ps ...predicate.Patient) *PatientQuery {
	pq.predicates = append(pq.predicates, ps...)
	return pq
}

// Limit the number of records to be returned by this query.
func (pq *PatientQuery) Limit(limit int) *PatientQuery {
	pq.ctx.Limit = &limit
	return pq
}

// Offset to start from.
func (pq *PatientQuery) Offset(offset int) *PatientQuery {
	pq.ctx.Offset = &offset
	return pq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pq *PatientQuery) Unique(unique bool) *PatientQuery {
	pq.ctx.Unique = &unique
	return pq
}

// Order specifies how the records should be ordered.
func (pq *PatientQuery) Order(o ...patient.OrderOption) *PatientQuery {
	pq.order = append(pq.order, o...)
	return pq
}

// QueryCurrentCustomer chains the current query on the "current_customer" edge.
func (pq *PatientQuery) QueryCurrentCustomer() *CustomerQuery {
	query := (&CustomerClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(customer.Table, customer.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patient.CurrentCustomerTable, patient.CurrentCustomerColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientCustomers chains the current query on the "patient_customers" edge.
func (pq *PatientQuery) QueryPatientCustomers() *CustomerQuery {
	query := (&CustomerClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(customer.Table, customer.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, patient.PatientCustomersTable, patient.PatientCustomersPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySamples chains the current query on the "samples" edge.
func (pq *PatientQuery) QuerySamples() *SampleQuery {
	query := (&SampleClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(sample.Table, sample.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.SamplesTable, patient.SamplesColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientContacts chains the current query on the "patient_contacts" edge.
func (pq *PatientQuery) QueryPatientContacts() *ContactQuery {
	query := (&ContactClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(contact.Table, contact.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.PatientContactsTable, patient.PatientContactsColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientAddresses chains the current query on the "patient_addresses" edge.
func (pq *PatientQuery) QueryPatientAddresses() *AddressQuery {
	query := (&AddressClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(address.Table, address.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.PatientAddressesTable, patient.PatientAddressesColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientClinics chains the current query on the "patient_clinics" edge.
func (pq *PatientQuery) QueryPatientClinics() *ClinicQuery {
	query := (&ClinicClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(clinic.Table, clinic.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, patient.PatientClinicsTable, patient.PatientClinicsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (pq *PatientQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, patient.UserTable, patient.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientWeightHeightHistory chains the current query on the "patient_weight_height_history" edge.
func (pq *PatientQuery) QueryPatientWeightHeightHistory() *PatientWeightHeightQuery {
	query := (&PatientWeightHeightClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(patientweightheight.Table, patientweightheight.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patient.PatientWeightHeightHistoryTable, patient.PatientWeightHeightHistoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientSettings chains the current query on the "patient_settings" edge.
func (pq *PatientQuery) QueryPatientSettings() *SettingQuery {
	query := (&SettingClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patient.Table, patient.FieldID, selector),
			sqlgraph.To(setting.Table, setting.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, patient.PatientSettingsTable, patient.PatientSettingsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Patient entity from the query.
// Returns a *NotFoundError when no Patient was found.
func (pq *PatientQuery) First(ctx context.Context) (*Patient, error) {
	nodes, err := pq.Limit(1).All(setContextOp(ctx, pq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{patient.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pq *PatientQuery) FirstX(ctx context.Context) *Patient {
	node, err := pq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Patient ID from the query.
// Returns a *NotFoundError when no Patient ID was found.
func (pq *PatientQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pq.Limit(1).IDs(setContextOp(ctx, pq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{patient.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pq *PatientQuery) FirstIDX(ctx context.Context) int {
	id, err := pq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Patient entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Patient entity is found.
// Returns a *NotFoundError when no Patient entities are found.
func (pq *PatientQuery) Only(ctx context.Context) (*Patient, error) {
	nodes, err := pq.Limit(2).All(setContextOp(ctx, pq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{patient.Label}
	default:
		return nil, &NotSingularError{patient.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pq *PatientQuery) OnlyX(ctx context.Context) *Patient {
	node, err := pq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Patient ID in the query.
// Returns a *NotSingularError when more than one Patient ID is found.
// Returns a *NotFoundError when no entities are found.
func (pq *PatientQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pq.Limit(2).IDs(setContextOp(ctx, pq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{patient.Label}
	default:
		err = &NotSingularError{patient.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pq *PatientQuery) OnlyIDX(ctx context.Context) int {
	id, err := pq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Patients.
func (pq *PatientQuery) All(ctx context.Context) ([]*Patient, error) {
	ctx = setContextOp(ctx, pq.ctx, "All")
	if err := pq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Patient, *PatientQuery]()
	return withInterceptors[[]*Patient](ctx, pq, qr, pq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pq *PatientQuery) AllX(ctx context.Context) []*Patient {
	nodes, err := pq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Patient IDs.
func (pq *PatientQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pq.ctx.Unique == nil && pq.path != nil {
		pq.Unique(true)
	}
	ctx = setContextOp(ctx, pq.ctx, "IDs")
	if err = pq.Select(patient.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pq *PatientQuery) IDsX(ctx context.Context) []int {
	ids, err := pq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pq *PatientQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pq.ctx, "Count")
	if err := pq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pq, querierCount[*PatientQuery](), pq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pq *PatientQuery) CountX(ctx context.Context) int {
	count, err := pq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pq *PatientQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pq.ctx, "Exist")
	switch _, err := pq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pq *PatientQuery) ExistX(ctx context.Context) bool {
	exist, err := pq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PatientQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pq *PatientQuery) Clone() *PatientQuery {
	if pq == nil {
		return nil
	}
	return &PatientQuery{
		config:                         pq.config,
		ctx:                            pq.ctx.Clone(),
		order:                          append([]patient.OrderOption{}, pq.order...),
		inters:                         append([]Interceptor{}, pq.inters...),
		predicates:                     append([]predicate.Patient{}, pq.predicates...),
		withCurrentCustomer:            pq.withCurrentCustomer.Clone(),
		withPatientCustomers:           pq.withPatientCustomers.Clone(),
		withSamples:                    pq.withSamples.Clone(),
		withPatientContacts:            pq.withPatientContacts.Clone(),
		withPatientAddresses:           pq.withPatientAddresses.Clone(),
		withPatientClinics:             pq.withPatientClinics.Clone(),
		withUser:                       pq.withUser.Clone(),
		withPatientWeightHeightHistory: pq.withPatientWeightHeightHistory.Clone(),
		withPatientSettings:            pq.withPatientSettings.Clone(),
		// clone intermediate query.
		sql:  pq.sql.Clone(),
		path: pq.path,
	}
}

// WithCurrentCustomer tells the query-builder to eager-load the nodes that are connected to
// the "current_customer" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithCurrentCustomer(opts ...func(*CustomerQuery)) *PatientQuery {
	query := (&CustomerClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withCurrentCustomer = query
	return pq
}

// WithPatientCustomers tells the query-builder to eager-load the nodes that are connected to
// the "patient_customers" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientCustomers(opts ...func(*CustomerQuery)) *PatientQuery {
	query := (&CustomerClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientCustomers = query
	return pq
}

// WithSamples tells the query-builder to eager-load the nodes that are connected to
// the "samples" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithSamples(opts ...func(*SampleQuery)) *PatientQuery {
	query := (&SampleClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withSamples = query
	return pq
}

// WithPatientContacts tells the query-builder to eager-load the nodes that are connected to
// the "patient_contacts" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientContacts(opts ...func(*ContactQuery)) *PatientQuery {
	query := (&ContactClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientContacts = query
	return pq
}

// WithPatientAddresses tells the query-builder to eager-load the nodes that are connected to
// the "patient_addresses" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientAddresses(opts ...func(*AddressQuery)) *PatientQuery {
	query := (&AddressClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientAddresses = query
	return pq
}

// WithPatientClinics tells the query-builder to eager-load the nodes that are connected to
// the "patient_clinics" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientClinics(opts ...func(*ClinicQuery)) *PatientQuery {
	query := (&ClinicClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientClinics = query
	return pq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithUser(opts ...func(*UserQuery)) *PatientQuery {
	query := (&UserClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withUser = query
	return pq
}

// WithPatientWeightHeightHistory tells the query-builder to eager-load the nodes that are connected to
// the "patient_weight_height_history" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientWeightHeightHistory(opts ...func(*PatientWeightHeightQuery)) *PatientQuery {
	query := (&PatientWeightHeightClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientWeightHeightHistory = query
	return pq
}

// WithPatientSettings tells the query-builder to eager-load the nodes that are connected to
// the "patient_settings" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PatientQuery) WithPatientSettings(opts ...func(*SettingQuery)) *PatientQuery {
	query := (&SettingClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPatientSettings = query
	return pq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Patient.Query().
//		GroupBy(patient.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pq *PatientQuery) GroupBy(field string, fields ...string) *PatientGroupBy {
	pq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PatientGroupBy{build: pq}
	grbuild.flds = &pq.ctx.Fields
	grbuild.label = patient.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.Patient.Query().
//		Select(patient.FieldUserID).
//		Scan(ctx, &v)
func (pq *PatientQuery) Select(fields ...string) *PatientSelect {
	pq.ctx.Fields = append(pq.ctx.Fields, fields...)
	sbuild := &PatientSelect{PatientQuery: pq}
	sbuild.label = patient.Label
	sbuild.flds, sbuild.scan = &pq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PatientSelect configured with the given aggregations.
func (pq *PatientQuery) Aggregate(fns ...AggregateFunc) *PatientSelect {
	return pq.Select().Aggregate(fns...)
}

func (pq *PatientQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pq); err != nil {
				return err
			}
		}
	}
	for _, f := range pq.ctx.Fields {
		if !patient.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pq.path != nil {
		prev, err := pq.path(ctx)
		if err != nil {
			return err
		}
		pq.sql = prev
	}
	return nil
}

func (pq *PatientQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Patient, error) {
	var (
		nodes       = []*Patient{}
		_spec       = pq.querySpec()
		loadedTypes = [9]bool{
			pq.withCurrentCustomer != nil,
			pq.withPatientCustomers != nil,
			pq.withSamples != nil,
			pq.withPatientContacts != nil,
			pq.withPatientAddresses != nil,
			pq.withPatientClinics != nil,
			pq.withUser != nil,
			pq.withPatientWeightHeightHistory != nil,
			pq.withPatientSettings != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Patient).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Patient{config: pq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pq.withCurrentCustomer; query != nil {
		if err := pq.loadCurrentCustomer(ctx, query, nodes, nil,
			func(n *Patient, e *Customer) { n.Edges.CurrentCustomer = e }); err != nil {
			return nil, err
		}
	}
	if query := pq.withPatientCustomers; query != nil {
		if err := pq.loadPatientCustomers(ctx, query, nodes,
			func(n *Patient) { n.Edges.PatientCustomers = []*Customer{} },
			func(n *Patient, e *Customer) { n.Edges.PatientCustomers = append(n.Edges.PatientCustomers, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withSamples; query != nil {
		if err := pq.loadSamples(ctx, query, nodes,
			func(n *Patient) { n.Edges.Samples = []*Sample{} },
			func(n *Patient, e *Sample) { n.Edges.Samples = append(n.Edges.Samples, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withPatientContacts; query != nil {
		if err := pq.loadPatientContacts(ctx, query, nodes,
			func(n *Patient) { n.Edges.PatientContacts = []*Contact{} },
			func(n *Patient, e *Contact) { n.Edges.PatientContacts = append(n.Edges.PatientContacts, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withPatientAddresses; query != nil {
		if err := pq.loadPatientAddresses(ctx, query, nodes,
			func(n *Patient) { n.Edges.PatientAddresses = []*Address{} },
			func(n *Patient, e *Address) { n.Edges.PatientAddresses = append(n.Edges.PatientAddresses, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withPatientClinics; query != nil {
		if err := pq.loadPatientClinics(ctx, query, nodes,
			func(n *Patient) { n.Edges.PatientClinics = []*Clinic{} },
			func(n *Patient, e *Clinic) { n.Edges.PatientClinics = append(n.Edges.PatientClinics, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withUser; query != nil {
		if err := pq.loadUser(ctx, query, nodes, nil,
			func(n *Patient, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := pq.withPatientWeightHeightHistory; query != nil {
		if err := pq.loadPatientWeightHeightHistory(ctx, query, nodes,
			func(n *Patient) { n.Edges.PatientWeightHeightHistory = []*PatientWeightHeight{} },
			func(n *Patient, e *PatientWeightHeight) {
				n.Edges.PatientWeightHeightHistory = append(n.Edges.PatientWeightHeightHistory, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := pq.withPatientSettings; query != nil {
		if err := pq.loadPatientSettings(ctx, query, nodes,
			func(n *Patient) { n.Edges.PatientSettings = []*Setting{} },
			func(n *Patient, e *Setting) { n.Edges.PatientSettings = append(n.Edges.PatientSettings, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pq *PatientQuery) loadCurrentCustomer(ctx context.Context, query *CustomerQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *Customer)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Patient)
	for i := range nodes {
		fk := nodes[i].CustomerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(customer.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "customer_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pq *PatientQuery) loadPatientCustomers(ctx context.Context, query *CustomerQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *Customer)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*Patient)
	nids := make(map[int]map[*Patient]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(patient.PatientCustomersTable)
		s.Join(joinT).On(s.C(customer.FieldID), joinT.C(patient.PatientCustomersPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(patient.PatientCustomersPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(patient.PatientCustomersPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*Patient]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Customer](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "patient_customers" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (pq *PatientQuery) loadSamples(ctx context.Context, query *SampleQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *Sample)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Patient)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(sample.FieldPatientID)
	}
	query.Where(predicate.Sample(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(patient.SamplesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PatientID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "patient_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (pq *PatientQuery) loadPatientContacts(ctx context.Context, query *ContactQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *Contact)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Patient)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(contact.FieldPatientID)
	}
	query.Where(predicate.Contact(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(patient.PatientContactsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PatientID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "patient_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (pq *PatientQuery) loadPatientAddresses(ctx context.Context, query *AddressQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *Address)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Patient)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(address.FieldPatientID)
	}
	query.Where(predicate.Address(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(patient.PatientAddressesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PatientID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "patient_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (pq *PatientQuery) loadPatientClinics(ctx context.Context, query *ClinicQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *Clinic)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*Patient)
	nids := make(map[int]map[*Patient]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(patient.PatientClinicsTable)
		s.Join(joinT).On(s.C(clinic.FieldID), joinT.C(patient.PatientClinicsPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(patient.PatientClinicsPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(patient.PatientClinicsPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*Patient]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Clinic](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "patient_clinics" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (pq *PatientQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Patient)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pq *PatientQuery) loadPatientWeightHeightHistory(ctx context.Context, query *PatientWeightHeightQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *PatientWeightHeight)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Patient)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(patientweightheight.FieldPatientID)
	}
	query.Where(predicate.PatientWeightHeight(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(patient.PatientWeightHeightHistoryColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PatientID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "patient_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (pq *PatientQuery) loadPatientSettings(ctx context.Context, query *SettingQuery, nodes []*Patient, init func(*Patient), assign func(*Patient, *Setting)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*Patient)
	nids := make(map[int]map[*Patient]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(patient.PatientSettingsTable)
		s.Join(joinT).On(s.C(setting.FieldID), joinT.C(patient.PatientSettingsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(patient.PatientSettingsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(patient.PatientSettingsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*Patient]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Setting](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "patient_settings" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (pq *PatientQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pq.querySpec()
	_spec.Node.Columns = pq.ctx.Fields
	if len(pq.ctx.Fields) > 0 {
		_spec.Unique = pq.ctx.Unique != nil && *pq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pq.driver, _spec)
}

func (pq *PatientQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(patient.Table, patient.Columns, sqlgraph.NewFieldSpec(patient.FieldID, field.TypeInt))
	_spec.From = pq.sql
	if unique := pq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pq.path != nil {
		_spec.Unique = true
	}
	if fields := pq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, patient.FieldID)
		for i := range fields {
			if fields[i] != patient.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pq.withCurrentCustomer != nil {
			_spec.Node.AddColumnOnce(patient.FieldCustomerID)
		}
		if pq.withUser != nil {
			_spec.Node.AddColumnOnce(patient.FieldUserID)
		}
	}
	if ps := pq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pq *PatientQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pq.driver.Dialect())
	t1 := builder.Table(patient.Table)
	columns := pq.ctx.Fields
	if len(columns) == 0 {
		columns = patient.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pq.sql != nil {
		selector = pq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pq.ctx.Unique != nil && *pq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pq.predicates {
		p(selector)
	}
	for _, p := range pq.order {
		p(selector)
	}
	if offset := pq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PatientGroupBy is the group-by builder for Patient entities.
type PatientGroupBy struct {
	selector
	build *PatientQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pgb *PatientGroupBy) Aggregate(fns ...AggregateFunc) *PatientGroupBy {
	pgb.fns = append(pgb.fns, fns...)
	return pgb
}

// Scan applies the selector query and scans the result into the given value.
func (pgb *PatientGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pgb.build.ctx, "GroupBy")
	if err := pgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PatientQuery, *PatientGroupBy](ctx, pgb.build, pgb, pgb.build.inters, v)
}

func (pgb *PatientGroupBy) sqlScan(ctx context.Context, root *PatientQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pgb.fns))
	for _, fn := range pgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pgb.flds)+len(pgb.fns))
		for _, f := range *pgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PatientSelect is the builder for selecting fields of Patient entities.
type PatientSelect struct {
	*PatientQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ps *PatientSelect) Aggregate(fns ...AggregateFunc) *PatientSelect {
	ps.fns = append(ps.fns, fns...)
	return ps
}

// Scan applies the selector query and scans the result into the given value.
func (ps *PatientSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ps.ctx, "Select")
	if err := ps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PatientQuery, *PatientSelect](ctx, ps.PatientQuery, ps, ps.inters, v)
}

func (ps *PatientSelect) sqlScan(ctx context.Context, root *PatientQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ps.fns))
	for _, fn := range ps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
