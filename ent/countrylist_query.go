// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"coresamples/ent/countrylist"
	"coresamples/ent/predicate"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CountryListQuery is the builder for querying CountryList entities.
type CountryListQuery struct {
	config
	ctx        *QueryContext
	order      []countrylist.OrderOption
	inters     []Interceptor
	predicates []predicate.CountryList
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CountryListQuery builder.
func (clq *CountryListQuery) Where(ps ...predicate.CountryList) *CountryListQuery {
	clq.predicates = append(clq.predicates, ps...)
	return clq
}

// Limit the number of records to be returned by this query.
func (clq *CountryListQuery) Limit(limit int) *CountryListQuery {
	clq.ctx.Limit = &limit
	return clq
}

// Offset to start from.
func (clq *CountryListQuery) Offset(offset int) *CountryListQuery {
	clq.ctx.Offset = &offset
	return clq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (clq *CountryListQuery) Unique(unique bool) *CountryListQuery {
	clq.ctx.Unique = &unique
	return clq
}

// Order specifies how the records should be ordered.
func (clq *CountryListQuery) Order(o ...countrylist.OrderOption) *CountryListQuery {
	clq.order = append(clq.order, o...)
	return clq
}

// First returns the first CountryList entity from the query.
// Returns a *NotFoundError when no CountryList was found.
func (clq *CountryListQuery) First(ctx context.Context) (*CountryList, error) {
	nodes, err := clq.Limit(1).All(setContextOp(ctx, clq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{countrylist.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (clq *CountryListQuery) FirstX(ctx context.Context) *CountryList {
	node, err := clq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CountryList ID from the query.
// Returns a *NotFoundError when no CountryList ID was found.
func (clq *CountryListQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = clq.Limit(1).IDs(setContextOp(ctx, clq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{countrylist.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (clq *CountryListQuery) FirstIDX(ctx context.Context) int {
	id, err := clq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CountryList entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CountryList entity is found.
// Returns a *NotFoundError when no CountryList entities are found.
func (clq *CountryListQuery) Only(ctx context.Context) (*CountryList, error) {
	nodes, err := clq.Limit(2).All(setContextOp(ctx, clq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{countrylist.Label}
	default:
		return nil, &NotSingularError{countrylist.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (clq *CountryListQuery) OnlyX(ctx context.Context) *CountryList {
	node, err := clq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CountryList ID in the query.
// Returns a *NotSingularError when more than one CountryList ID is found.
// Returns a *NotFoundError when no entities are found.
func (clq *CountryListQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = clq.Limit(2).IDs(setContextOp(ctx, clq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{countrylist.Label}
	default:
		err = &NotSingularError{countrylist.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (clq *CountryListQuery) OnlyIDX(ctx context.Context) int {
	id, err := clq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CountryLists.
func (clq *CountryListQuery) All(ctx context.Context) ([]*CountryList, error) {
	ctx = setContextOp(ctx, clq.ctx, "All")
	if err := clq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CountryList, *CountryListQuery]()
	return withInterceptors[[]*CountryList](ctx, clq, qr, clq.inters)
}

// AllX is like All, but panics if an error occurs.
func (clq *CountryListQuery) AllX(ctx context.Context) []*CountryList {
	nodes, err := clq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CountryList IDs.
func (clq *CountryListQuery) IDs(ctx context.Context) (ids []int, err error) {
	if clq.ctx.Unique == nil && clq.path != nil {
		clq.Unique(true)
	}
	ctx = setContextOp(ctx, clq.ctx, "IDs")
	if err = clq.Select(countrylist.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (clq *CountryListQuery) IDsX(ctx context.Context) []int {
	ids, err := clq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (clq *CountryListQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, clq.ctx, "Count")
	if err := clq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, clq, querierCount[*CountryListQuery](), clq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (clq *CountryListQuery) CountX(ctx context.Context) int {
	count, err := clq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (clq *CountryListQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, clq.ctx, "Exist")
	switch _, err := clq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (clq *CountryListQuery) ExistX(ctx context.Context) bool {
	exist, err := clq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CountryListQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (clq *CountryListQuery) Clone() *CountryListQuery {
	if clq == nil {
		return nil
	}
	return &CountryListQuery{
		config:     clq.config,
		ctx:        clq.ctx.Clone(),
		order:      append([]countrylist.OrderOption{}, clq.order...),
		inters:     append([]Interceptor{}, clq.inters...),
		predicates: append([]predicate.CountryList{}, clq.predicates...),
		// clone intermediate query.
		sql:  clq.sql.Clone(),
		path: clq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CountryName string `json:"country_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CountryList.Query().
//		GroupBy(countrylist.FieldCountryName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (clq *CountryListQuery) GroupBy(field string, fields ...string) *CountryListGroupBy {
	clq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CountryListGroupBy{build: clq}
	grbuild.flds = &clq.ctx.Fields
	grbuild.label = countrylist.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CountryName string `json:"country_name,omitempty"`
//	}
//
//	client.CountryList.Query().
//		Select(countrylist.FieldCountryName).
//		Scan(ctx, &v)
func (clq *CountryListQuery) Select(fields ...string) *CountryListSelect {
	clq.ctx.Fields = append(clq.ctx.Fields, fields...)
	sbuild := &CountryListSelect{CountryListQuery: clq}
	sbuild.label = countrylist.Label
	sbuild.flds, sbuild.scan = &clq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CountryListSelect configured with the given aggregations.
func (clq *CountryListQuery) Aggregate(fns ...AggregateFunc) *CountryListSelect {
	return clq.Select().Aggregate(fns...)
}

func (clq *CountryListQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range clq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, clq); err != nil {
				return err
			}
		}
	}
	for _, f := range clq.ctx.Fields {
		if !countrylist.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if clq.path != nil {
		prev, err := clq.path(ctx)
		if err != nil {
			return err
		}
		clq.sql = prev
	}
	return nil
}

func (clq *CountryListQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CountryList, error) {
	var (
		nodes = []*CountryList{}
		_spec = clq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CountryList).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CountryList{config: clq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, clq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (clq *CountryListQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := clq.querySpec()
	_spec.Node.Columns = clq.ctx.Fields
	if len(clq.ctx.Fields) > 0 {
		_spec.Unique = clq.ctx.Unique != nil && *clq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, clq.driver, _spec)
}

func (clq *CountryListQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(countrylist.Table, countrylist.Columns, sqlgraph.NewFieldSpec(countrylist.FieldID, field.TypeInt))
	_spec.From = clq.sql
	if unique := clq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if clq.path != nil {
		_spec.Unique = true
	}
	if fields := clq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, countrylist.FieldID)
		for i := range fields {
			if fields[i] != countrylist.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := clq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := clq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := clq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := clq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (clq *CountryListQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(clq.driver.Dialect())
	t1 := builder.Table(countrylist.Table)
	columns := clq.ctx.Fields
	if len(columns) == 0 {
		columns = countrylist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if clq.sql != nil {
		selector = clq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if clq.ctx.Unique != nil && *clq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range clq.predicates {
		p(selector)
	}
	for _, p := range clq.order {
		p(selector)
	}
	if offset := clq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := clq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CountryListGroupBy is the group-by builder for CountryList entities.
type CountryListGroupBy struct {
	selector
	build *CountryListQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (clgb *CountryListGroupBy) Aggregate(fns ...AggregateFunc) *CountryListGroupBy {
	clgb.fns = append(clgb.fns, fns...)
	return clgb
}

// Scan applies the selector query and scans the result into the given value.
func (clgb *CountryListGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, clgb.build.ctx, "GroupBy")
	if err := clgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CountryListQuery, *CountryListGroupBy](ctx, clgb.build, clgb, clgb.build.inters, v)
}

func (clgb *CountryListGroupBy) sqlScan(ctx context.Context, root *CountryListQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(clgb.fns))
	for _, fn := range clgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*clgb.flds)+len(clgb.fns))
		for _, f := range *clgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*clgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := clgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CountryListSelect is the builder for selecting fields of CountryList entities.
type CountryListSelect struct {
	*CountryListQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cls *CountryListSelect) Aggregate(fns ...AggregateFunc) *CountryListSelect {
	cls.fns = append(cls.fns, fns...)
	return cls
}

// Scan applies the selector query and scans the result into the given value.
func (cls *CountryListSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cls.ctx, "Select")
	if err := cls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CountryListQuery, *CountryListSelect](ctx, cls.CountryListQuery, cls, cls.inters, v)
}

func (cls *CountryListSelect) sqlScan(ctx context.Context, root *CountryListQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cls.fns))
	for _, fn := range cls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
