// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"coresamples/ent/accountsubscription"
	"coresamples/ent/predicate"
	"coresamples/ent/serviceship"
	"coresamples/ent/serviceshipbillingplan"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ServiceshipQuery is the builder for querying Serviceship entities.
type ServiceshipQuery struct {
	config
	ctx                        *QueryContext
	order                      []serviceship.OrderOption
	inters                     []Interceptor
	predicates                 []predicate.Serviceship
	withServiceshipBillingPlan *ServiceshipBillingPlanQuery
	withAccountSubscription    *AccountSubscriptionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ServiceshipQuery builder.
func (sq *ServiceshipQuery) Where(ps ...predicate.Serviceship) *ServiceshipQuery {
	sq.predicates = append(sq.predicates, ps...)
	return sq
}

// Limit the number of records to be returned by this query.
func (sq *ServiceshipQuery) Limit(limit int) *ServiceshipQuery {
	sq.ctx.Limit = &limit
	return sq
}

// Offset to start from.
func (sq *ServiceshipQuery) Offset(offset int) *ServiceshipQuery {
	sq.ctx.Offset = &offset
	return sq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sq *ServiceshipQuery) Unique(unique bool) *ServiceshipQuery {
	sq.ctx.Unique = &unique
	return sq
}

// Order specifies how the records should be ordered.
func (sq *ServiceshipQuery) Order(o ...serviceship.OrderOption) *ServiceshipQuery {
	sq.order = append(sq.order, o...)
	return sq
}

// QueryServiceshipBillingPlan chains the current query on the "serviceship_billing_plan" edge.
func (sq *ServiceshipQuery) QueryServiceshipBillingPlan() *ServiceshipBillingPlanQuery {
	query := (&ServiceshipBillingPlanClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(serviceship.Table, serviceship.FieldID, selector),
			sqlgraph.To(serviceshipbillingplan.Table, serviceshipbillingplan.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, serviceship.ServiceshipBillingPlanTable, serviceship.ServiceshipBillingPlanColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAccountSubscription chains the current query on the "account_subscription" edge.
func (sq *ServiceshipQuery) QueryAccountSubscription() *AccountSubscriptionQuery {
	query := (&AccountSubscriptionClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(serviceship.Table, serviceship.FieldID, selector),
			sqlgraph.To(accountsubscription.Table, accountsubscription.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, serviceship.AccountSubscriptionTable, serviceship.AccountSubscriptionColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Serviceship entity from the query.
// Returns a *NotFoundError when no Serviceship was found.
func (sq *ServiceshipQuery) First(ctx context.Context) (*Serviceship, error) {
	nodes, err := sq.Limit(1).All(setContextOp(ctx, sq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{serviceship.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sq *ServiceshipQuery) FirstX(ctx context.Context) *Serviceship {
	node, err := sq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Serviceship ID from the query.
// Returns a *NotFoundError when no Serviceship ID was found.
func (sq *ServiceshipQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(1).IDs(setContextOp(ctx, sq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{serviceship.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sq *ServiceshipQuery) FirstIDX(ctx context.Context) int {
	id, err := sq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Serviceship entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Serviceship entity is found.
// Returns a *NotFoundError when no Serviceship entities are found.
func (sq *ServiceshipQuery) Only(ctx context.Context) (*Serviceship, error) {
	nodes, err := sq.Limit(2).All(setContextOp(ctx, sq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{serviceship.Label}
	default:
		return nil, &NotSingularError{serviceship.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sq *ServiceshipQuery) OnlyX(ctx context.Context) *Serviceship {
	node, err := sq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Serviceship ID in the query.
// Returns a *NotSingularError when more than one Serviceship ID is found.
// Returns a *NotFoundError when no entities are found.
func (sq *ServiceshipQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(2).IDs(setContextOp(ctx, sq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{serviceship.Label}
	default:
		err = &NotSingularError{serviceship.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sq *ServiceshipQuery) OnlyIDX(ctx context.Context) int {
	id, err := sq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Serviceships.
func (sq *ServiceshipQuery) All(ctx context.Context) ([]*Serviceship, error) {
	ctx = setContextOp(ctx, sq.ctx, "All")
	if err := sq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Serviceship, *ServiceshipQuery]()
	return withInterceptors[[]*Serviceship](ctx, sq, qr, sq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sq *ServiceshipQuery) AllX(ctx context.Context) []*Serviceship {
	nodes, err := sq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Serviceship IDs.
func (sq *ServiceshipQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sq.ctx.Unique == nil && sq.path != nil {
		sq.Unique(true)
	}
	ctx = setContextOp(ctx, sq.ctx, "IDs")
	if err = sq.Select(serviceship.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sq *ServiceshipQuery) IDsX(ctx context.Context) []int {
	ids, err := sq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sq *ServiceshipQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sq.ctx, "Count")
	if err := sq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sq, querierCount[*ServiceshipQuery](), sq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sq *ServiceshipQuery) CountX(ctx context.Context) int {
	count, err := sq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sq *ServiceshipQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sq.ctx, "Exist")
	switch _, err := sq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sq *ServiceshipQuery) ExistX(ctx context.Context) bool {
	exist, err := sq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ServiceshipQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sq *ServiceshipQuery) Clone() *ServiceshipQuery {
	if sq == nil {
		return nil
	}
	return &ServiceshipQuery{
		config:                     sq.config,
		ctx:                        sq.ctx.Clone(),
		order:                      append([]serviceship.OrderOption{}, sq.order...),
		inters:                     append([]Interceptor{}, sq.inters...),
		predicates:                 append([]predicate.Serviceship{}, sq.predicates...),
		withServiceshipBillingPlan: sq.withServiceshipBillingPlan.Clone(),
		withAccountSubscription:    sq.withAccountSubscription.Clone(),
		// clone intermediate query.
		sql:  sq.sql.Clone(),
		path: sq.path,
	}
}

// WithServiceshipBillingPlan tells the query-builder to eager-load the nodes that are connected to
// the "serviceship_billing_plan" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ServiceshipQuery) WithServiceshipBillingPlan(opts ...func(*ServiceshipBillingPlanQuery)) *ServiceshipQuery {
	query := (&ServiceshipBillingPlanClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withServiceshipBillingPlan = query
	return sq
}

// WithAccountSubscription tells the query-builder to eager-load the nodes that are connected to
// the "account_subscription" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ServiceshipQuery) WithAccountSubscription(opts ...func(*AccountSubscriptionQuery)) *ServiceshipQuery {
	query := (&AccountSubscriptionClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withAccountSubscription = query
	return sq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Tag string `json:"tag,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Serviceship.Query().
//		GroupBy(serviceship.FieldTag).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sq *ServiceshipQuery) GroupBy(field string, fields ...string) *ServiceshipGroupBy {
	sq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ServiceshipGroupBy{build: sq}
	grbuild.flds = &sq.ctx.Fields
	grbuild.label = serviceship.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Tag string `json:"tag,omitempty"`
//	}
//
//	client.Serviceship.Query().
//		Select(serviceship.FieldTag).
//		Scan(ctx, &v)
func (sq *ServiceshipQuery) Select(fields ...string) *ServiceshipSelect {
	sq.ctx.Fields = append(sq.ctx.Fields, fields...)
	sbuild := &ServiceshipSelect{ServiceshipQuery: sq}
	sbuild.label = serviceship.Label
	sbuild.flds, sbuild.scan = &sq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ServiceshipSelect configured with the given aggregations.
func (sq *ServiceshipQuery) Aggregate(fns ...AggregateFunc) *ServiceshipSelect {
	return sq.Select().Aggregate(fns...)
}

func (sq *ServiceshipQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sq); err != nil {
				return err
			}
		}
	}
	for _, f := range sq.ctx.Fields {
		if !serviceship.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sq.path != nil {
		prev, err := sq.path(ctx)
		if err != nil {
			return err
		}
		sq.sql = prev
	}
	return nil
}

func (sq *ServiceshipQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Serviceship, error) {
	var (
		nodes       = []*Serviceship{}
		_spec       = sq.querySpec()
		loadedTypes = [2]bool{
			sq.withServiceshipBillingPlan != nil,
			sq.withAccountSubscription != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Serviceship).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Serviceship{config: sq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sq.withServiceshipBillingPlan; query != nil {
		if err := sq.loadServiceshipBillingPlan(ctx, query, nodes,
			func(n *Serviceship) { n.Edges.ServiceshipBillingPlan = []*ServiceshipBillingPlan{} },
			func(n *Serviceship, e *ServiceshipBillingPlan) {
				n.Edges.ServiceshipBillingPlan = append(n.Edges.ServiceshipBillingPlan, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := sq.withAccountSubscription; query != nil {
		if err := sq.loadAccountSubscription(ctx, query, nodes,
			func(n *Serviceship) { n.Edges.AccountSubscription = []*AccountSubscription{} },
			func(n *Serviceship, e *AccountSubscription) {
				n.Edges.AccountSubscription = append(n.Edges.AccountSubscription, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sq *ServiceshipQuery) loadServiceshipBillingPlan(ctx context.Context, query *ServiceshipBillingPlanQuery, nodes []*Serviceship, init func(*Serviceship), assign func(*Serviceship, *ServiceshipBillingPlan)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Serviceship)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ServiceshipBillingPlan(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(serviceship.ServiceshipBillingPlanColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.serviceship_serviceship_billing_plan
		if fk == nil {
			return fmt.Errorf(`foreign-key "serviceship_serviceship_billing_plan" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "serviceship_serviceship_billing_plan" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (sq *ServiceshipQuery) loadAccountSubscription(ctx context.Context, query *AccountSubscriptionQuery, nodes []*Serviceship, init func(*Serviceship), assign func(*Serviceship, *AccountSubscription)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Serviceship)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.AccountSubscription(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(serviceship.AccountSubscriptionColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.serviceship_account_subscription
		if fk == nil {
			return fmt.Errorf(`foreign-key "serviceship_account_subscription" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "serviceship_account_subscription" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (sq *ServiceshipQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sq.querySpec()
	_spec.Node.Columns = sq.ctx.Fields
	if len(sq.ctx.Fields) > 0 {
		_spec.Unique = sq.ctx.Unique != nil && *sq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sq.driver, _spec)
}

func (sq *ServiceshipQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(serviceship.Table, serviceship.Columns, sqlgraph.NewFieldSpec(serviceship.FieldID, field.TypeInt))
	_spec.From = sq.sql
	if unique := sq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sq.path != nil {
		_spec.Unique = true
	}
	if fields := sq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, serviceship.FieldID)
		for i := range fields {
			if fields[i] != serviceship.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sq *ServiceshipQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sq.driver.Dialect())
	t1 := builder.Table(serviceship.Table)
	columns := sq.ctx.Fields
	if len(columns) == 0 {
		columns = serviceship.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sq.sql != nil {
		selector = sq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sq.ctx.Unique != nil && *sq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sq.predicates {
		p(selector)
	}
	for _, p := range sq.order {
		p(selector)
	}
	if offset := sq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ServiceshipGroupBy is the group-by builder for Serviceship entities.
type ServiceshipGroupBy struct {
	selector
	build *ServiceshipQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sgb *ServiceshipGroupBy) Aggregate(fns ...AggregateFunc) *ServiceshipGroupBy {
	sgb.fns = append(sgb.fns, fns...)
	return sgb
}

// Scan applies the selector query and scans the result into the given value.
func (sgb *ServiceshipGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sgb.build.ctx, "GroupBy")
	if err := sgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceshipQuery, *ServiceshipGroupBy](ctx, sgb.build, sgb, sgb.build.inters, v)
}

func (sgb *ServiceshipGroupBy) sqlScan(ctx context.Context, root *ServiceshipQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sgb.fns))
	for _, fn := range sgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sgb.flds)+len(sgb.fns))
		for _, f := range *sgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ServiceshipSelect is the builder for selecting fields of Serviceship entities.
type ServiceshipSelect struct {
	*ServiceshipQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ss *ServiceshipSelect) Aggregate(fns ...AggregateFunc) *ServiceshipSelect {
	ss.fns = append(ss.fns, fns...)
	return ss
}

// Scan applies the selector query and scans the result into the given value.
func (ss *ServiceshipSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ss.ctx, "Select")
	if err := ss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceshipQuery, *ServiceshipSelect](ctx, ss.ServiceshipQuery, ss, ss.inters, v)
}

func (ss *ServiceshipSelect) sqlScan(ctx context.Context, root *ServiceshipQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ss.fns))
	for _, fn := range ss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
